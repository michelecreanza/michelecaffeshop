<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Coffe</title>
  </head>
  <link rel="stylesheet" href="/css/main.css" />

  <body>
    <h1>Welcome to the Italian caffe!</h1>


    <h1>
      <% if(currentUser) { %> Welcome <%=currentUser.username%>! <% } else { %>
    <% } %>
    </h1>
    <% if(currentUser) { %> <% } %>

    <nav>
      <ul>
        <li><a href="/sessions/new">Log In</a></li>
        <li><a href="/users/new">Register</a></li>
      </ul>
      <% if(currentUser) { %>
      
        <form action="/sessions?_method=DELETE" method="POST">
          <input type="submit" value="Logout" />
        </form>
      </li>
      <% } %>
    </nav>
    <img
      src="https://29inth3cxa642murjs2o961f-wpengine.netdna-ssl.com/wp-content/uploads/2019/07/image-1.png"
      alt="HTML5 Icon"
      style="width:1600px;height:400px;"
    />

    

    <div class="containe  r">
      <div class="products">
        <% products.forEach( function (product) { %>
        
      
    
        <div class="card">
          <h5><%= product.name  %></h5>
          <a href="/products/<%=product.id%>">
            <img src="<%= product.img %>" alt="An image of <%= product.name %>"
          /></a>
          <h6>Price: $<%=product.price %></h6>
        </div>
        <% }); %>
      </div>
    </div>

    <div class="mapouter"><div class="gmap_canvas"><iframe width="800" height="800" id="gmap_canvas" src="https://maps.google.com/maps?q=stamford%20ct&t=&z=13&ie=UTF8&iwloc=&output=embed" frameborder="0" scrolling="no" marginheight="0" marginwidth="0"></iframe><a href="https://www.pureblack.de"></a></div><style>.mapouter{position:relative;text-align:right;height:500px;width:600px;}.gmap_canvas {overflow:hidden;background:none!important;height:500px;width:1100px;}</style></div>

    <nav>
      <a href="/products/new">ADD a New products</a>
    </nav>
  </body>
</html>
